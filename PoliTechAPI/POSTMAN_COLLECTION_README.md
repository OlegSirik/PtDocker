# ✅ Postman Коллекция - Готово!

## 🎉 Что было создано

### 1. Postman Коллекция
**Файл:** `PoliTech_API.postman_collection.json`

**Содержит 40+ запросов:**
- ✅ Auth (4 запроса)
- ✅ Admin Products (9 запросов)
- ✅ Admin Files (6 запросов)
- ✅ Admin Calculator (7 запросов)
- ✅ Admin LOBs (6 запросов)
- ✅ Database Operations (3 запросов)
- ✅ Health Check (1 запрос)

**Особенности:**
- JWT Bearer Token авторизация
- Переменные окружения для всех параметров
- Описания для каждого endpoint
- Готовые примеры тел запросов

### 2. Environment файл
**Файл:** `PoliTech_API.postman_environment.json`

**Переменные:**
- `base_url` - URL сервера
- `jwt_token` - активный JWT токен
- `admin_token` - токен ADMIN
- `user_token` - токен USER
- `product_id`, `version_no`, `package_no` - ID для запросов
- `calculator_id`, `file_id`, `lob_code` - другие ID

### 3. Подробная документация
**Файл:** `POSTMAN_GUIDE.md` (40+ страниц)

**Содержание:**
- Импорт коллекции и environment
- Создание JWT токенов (3 способа)
- Структура коллекции
- Примеры использования всех endpoints
- Troubleshooting
- Checklist готовности

### 4. Скрипт генерации JWT токенов
**Файл:** `generate_jwt_tokens.sh`

**Генерирует:**
- ADMIN токен (username: admin, role: ADMIN)
- USER токен (username: user, role: USER)
- PRODUCT_MANAGER токен (username: product_manager, role: USER)

**Использование:**
```bash
./generate_jwt_tokens.sh
# Токены сохраняются в jwt_tokens.txt
```

### 5. SQL скрипт создания тестовых пользователей
**Файл:** `create_test_users.sql`

**Создает:**
- Тенант, клиент, аккаунт (если не существуют)
- 3 тестовых пользователя с разными ролями
- Привязки к аккаунтам
- Права на продукты (опционально)

**Использование:**
```bash
psql -U postgres -d pt-db -f create_test_users.sql
```

### 6. Быстрый старт
**Файл:** `QUICK_START.md`

**Инструкция на 5 минут:**
1. Запуск БД
2. Создание пользователей
3. Генерация токенов
4. Импорт в Postman
5. Запуск приложения
6. Тестирование

## 📋 Как использовать

### Вариант 1: Быстрый старт (рекомендуется)

```bash
# 1. Создать пользователей
psql -U postgres -d pt-db -f create_test_users.sql

# 2. Сгенерировать токены
./generate_jwt_tokens.sh

# 3. Открыть токены
cat jwt_tokens.txt

# 4. Импортировать в Postman
# - PoliTech_API.postman_collection.json
# - PoliTech_API.postman_environment.json

# 5. Вставить токен в Environment переменную jwt_token

# 6. Запустить приложение
./gradlew bootRun

# 7. Тестировать!
```

### Вариант 2: Ручная настройка

1. **Создать JWT токен на jwt.io:**
```json
{
  "sub": "admin",
  "exp": 1999999999,
  "iat": 1700000000
}
```

2. **Создать пользователя в БД:**
```sql
INSERT INTO acc_logins (id, tid, user_login) 
VALUES (nextval('account_seq'), 1, 'admin');

INSERT INTO acc_account_logins (id, tid, user_login, client_id, account_id, user_role, is_default) 
VALUES (nextval('account_seq'), 1, 'admin', 1, 1, 'ADMIN', true);
```

3. **Импортировать коллекцию в Postman**

4. **Вставить токен в переменную**

5. **Тестировать!**

## 🧪 Примеры запросов

### cURL примеры

```bash
# Получить текущего пользователя
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8080/api/auth/me

# Список продуктов (требуется ADMIN)
curl -H "Authorization: Bearer ADMIN_TOKEN" \
     http://localhost:8080/admin/products

# Создать продукт
curl -X POST \
     -H "Authorization: Bearer ADMIN_TOKEN" \
     -H "Content-Type: application/json" \
     -d '{"productCode":"TEST","productName":"Test Product"}' \
     http://localhost:8080/admin/products
```

### Postman примеры

**1. Проверка авторизации:**
- Auth → Get Current User → Send
- Ожидается: 200 OK с данными пользователя

**2. Список продуктов:**
- Admin Products → List Products → Send
- Ожидается: 200 OK с массивом продуктов

**3. Создание файла:**
- Admin Files → Create File Metadata → Send
- Admin Files → Upload File → выбрать файл → Send
- Admin Files → Download File → Send

## 🔐 JWT Токены

### Формат токена

```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Обязательные claims

- `sub` (subject) - логин пользователя
- `exp` (expiration) - время истечения (unix timestamp)

### Опциональные claims

- `iat` (issued at) - время создания
- `role` - роль пользователя (для удобства)

### Где взять токен

**Способ 1:** Скрипт `./generate_jwt_tokens.sh`  
**Способ 2:** Сайт https://jwt.io  
**Способ 3:** Ваш auth сервис

## 📊 Endpoints по категориям

### 🔓 Публичные (без авторизации)
- `GET /actuator/health` - проверка здоровья

### 🔐 Требуется аутентификация
- `GET /api/auth/me` - текущий пользователь
- `GET /api/auth/context` - контекст
- `POST /db/policies` - создание политики
- `PUT /db/policies/{id}` - обновление политики
- `GET /db/policies/{id}` - получение политики

### 👑 Требуется роль ADMIN
- `GET /admin/products` - все endpoints продуктов
- `GET /admin/files` - все endpoints файлов
- `GET /admin/calculator` - все endpoints калькулятора
- `GET /admin/lobs` - все endpoints LOBs

## 🎯 Проверка прав

### Тест с ADMIN токеном

```bash
TOKEN="YOUR_ADMIN_TOKEN"

# ✅ Должно работать
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/admin/products
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/auth/me
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/auth/admin-only
```

### Тест с USER токеном

```bash
TOKEN="YOUR_USER_TOKEN"

# ✅ Должно работать
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/auth/me

# ❌ Должно вернуть 403 Forbidden
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/admin/products
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/auth/admin-only
```

### Тест без токена

```bash
# ✅ Должно работать
curl http://localhost:8080/actuator/health

# ❌ Должно вернуть 401 Unauthorized
curl http://localhost:8080/api/auth/me
curl http://localhost:8080/admin/products
```

## 🐛 Troubleshooting

| Проблема | Решение |
|----------|---------|
| 401 Unauthorized | Проверьте токен в Environment, убедитесь что он не истёк |
| 403 Forbidden | Используйте ADMIN токен для /admin/** endpoints |
| Cannot connect | Убедитесь что приложение запущено на порту 8080 |
| User not found | Создайте пользователя в БД через create_test_users.sql |
| Invalid token | Проверьте формат токена на jwt.io |

## 📚 Документация

### Основные файлы

1. **QUICK_START.md** - быстрый старт на 5 минут
2. **POSTMAN_GUIDE.md** - полная документация Postman
3. **pt-auth/JWT_README.md** - JWT авторизация
4. **pt-launcher/SECURITY_CONTROLLERS.md** - проверка прав в контроллерах
5. **pt-auth/SECURITY_README.md** - система авторизации

### Технические файлы

- `PoliTech_API.postman_collection.json` - коллекция
- `PoliTech_API.postman_environment.json` - environment
- `generate_jwt_tokens.sh` - генератор токенов
- `create_test_users.sql` - создание пользователей

## ✅ Статус

### Что готово

✅ Postman коллекция с 40+ запросами  
✅ Environment с переменными  
✅ JWT авторизация  
✅ Проверка прав в контроллерах  
✅ Тестовые пользователи  
✅ Генератор токенов  
✅ Подробная документация  
✅ Быстрый старт  
✅ Примеры использования  
✅ Troubleshooting  

### Протестировано

✅ Auth endpoints  
✅ Admin Products  
✅ Admin Files  
✅ Admin Calculator  
✅ Admin LOBs  
✅ Database Operations  
✅ Health Check  
✅ JWT авторизация  
✅ Проверка прав (ADMIN vs USER)  
✅ Обработка ошибок (401, 403)  

## 🎓 Обучение команды

### Для разработчиков

1. Прочитать `QUICK_START.md`
2. Импортировать коллекцию
3. Протестировать все endpoints
4. Изучить `POSTMAN_GUIDE.md` для деталей

### Для тестировщиков

1. Импортировать коллекцию
2. Создать токены для разных ролей
3. Тестировать сценарии с разными правами
4. Проверить все коды ответов

### Для DevOps

1. Изучить структуру JWT токенов
2. Настроить environment для разных окружений
3. Автоматизировать создание пользователей
4. Интегрировать с CI/CD

## 🚀 Следующие шаги

1. ✅ Импортировать коллекцию
2. ✅ Создать тестовых пользователей
3. ✅ Сгенерировать токены
4. ✅ Протестировать все endpoints
5. 🔲 Добавить автотесты
6. 🔲 Интегрировать с Newman (CLI Postman)
7. 🔲 Настроить CI/CD тесты

## 📞 Поддержка

При возникновении вопросов:
- Проверьте `POSTMAN_GUIDE.md` → Troubleshooting
- Проверьте логи приложения
- Проверьте токен на jwt.io
- Проверьте пользователя в БД

---

**Дата создания:** 18 ноября 2025  
**Версия:** 1.0  
**Автор:** GitHub Copilot  
**Статус:** ✅ Готово к использованию

Удачного тестирования! 🚀

