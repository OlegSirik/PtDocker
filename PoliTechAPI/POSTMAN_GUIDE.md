# Postman Коллекция для PoliTech API

## 📦 Файлы коллекции

- **PoliTech_API.postman_collection.json** - основная коллекция с запросами
- **PoliTech_API.postman_environment.json** - environment с переменными

## 🚀 Импорт в Postman

### 1. Импорт коллекции

1. Откройте Postman
2. Нажмите **Import** в левом верхнем углу
3. Перетащите файл `PoliTech_API.postman_collection.json` или выберите его через **Select Files**
4. Нажмите **Import**

### 2. Импорт Environment

1. Нажмите **Import**
2. Перетащите файл `PoliTech_API.postman_environment.json`
3. Нажмите **Import**
4. Выберите **PoliTech API Environment** в выпадающем списке окружений (правый верхний угол)

## 🔑 Настройка JWT токенов

### Способ 1: Создание токена на jwt.io

1. Перейдите на https://jwt.io
2. В разделе **Payload** вставьте:

```json
{
  "sub": "admin",
  "exp": 1999999999,
  "iat": 1700000000,
  "role": "ADMIN"
}
```

3. Скопируйте сгенерированный токен
4. В Postman откройте **Environments** → **PoliTech API Environment**
5. Замените значение переменной `jwt_token` или `admin_token` на скопированный токен

### Способ 2: Токены для разных ролей

#### ADMIN токен (полный доступ)
```json
{
  "sub": "admin",
  "exp": 1999999999,
  "iat": 1700000000,
  "role": "ADMIN"
}
```

#### USER токен (ограниченный доступ)
```json
{
  "sub": "user",
  "exp": 1999999999,
  "iat": 1700000000,
  "role": "USER"
}
```

#### Токен с правами на продукт
```json
{
  "sub": "product_manager",
  "exp": 1999999999,
  "iat": 1700000000,
  "role": "USER",
  "productRoles": ["TEST_PRODUCT_READ", "TEST_PRODUCT_QUOTE"]
}
```

### Способ 3: Реальные JWT токены

Если у вас есть реальная система генерации JWT токенов:

1. Получите токен от вашего auth сервиса
2. Обновите переменную `jwt_token` в Environment
3. Токен должен содержать обязательные поля:
   - `sub` (subject) - логин пользователя
   - `exp` (expiration) - время истечения (unix timestamp)

## 📋 Структура коллекции

### 1. Auth (Аутентификация)
- **Get Current User** - получить информацию о текущем пользователе
- **Get User Context** - получить контекст через SecurityContextHelper
- **Check Product Access** - проверить доступ к продуктам
- **Admin Only Endpoint** - endpoint только для ADMIN

### 2. Admin Products (Управление продуктами)
Все endpoints требуют роль **ADMIN**:
- List Products - список продуктов
- Create Product - создание продукта
- Get Product Version - получение версии
- Update Product Version - обновление версии
- Create New Version - создание новой версии
- Delete Product - удаление продукта
- Delete Product Version - удаление версии
- Get Quote Example JSON - пример JSON для расчёта
- Get Save Example JSON - пример JSON для сохранения

### 3. Admin Files (Управление файлами)
Все endpoints требуют роль **ADMIN**:
- List Files - список файлов
- Create File Metadata - создание метаданных
- Upload File - загрузка файла
- Download File - скачивание файла
- Process File - обработка файла с переменными
- Delete File - удаление файла

### 4. Admin Calculator (Управление калькуляторами)
Все endpoints требуют роль **ADMIN**:
- Get Calculator - получение калькулятора
- Create Calculator - создание калькулятора
- Update Calculator - обновление калькулятора
- Get Coefficients - получение таблицы коэффициентов
- Create Coefficients - создание коэффициентов
- Replace Coefficients - замена коэффициентов
- Sync Calculator Variables - синхронизация переменных

### 5. Admin LOBs (Управление линиями бизнеса)
Все endpoints требуют роль **ADMIN**:
- List LOBs - список LOBs
- Get LOB by Code - получение LOB
- Create LOB - создание LOB
- Update LOB - обновление LOB
- Delete LOB - удаление LOB
- Get LOB Example JSON - пример JSON

### 6. Database Operations (Операции с БД)
Требуется **аутентификация** (любая роль):
- Create Policy - создание политики
- Update Policy - обновление политики
- Get Policy by ID - получение политики

### 7. Health Check (Проверка здоровья)
- Actuator Health - без авторизации

## 🔐 Авторизация

Все запросы (кроме Health Check) используют **Bearer Token** авторизацию:

```
Authorization: Bearer {{jwt_token}}
```

Токен автоматически подставляется из переменной окружения `jwt_token`.

### Смена токена для конкретного запроса

Если нужно протестировать с другим токеном:

1. Откройте запрос
2. Перейдите на вкладку **Authorization**
3. Выберите **Inherit auth from parent** или **Bearer Token**
4. Замените `{{jwt_token}}` на `{{user_token}}` или другую переменную

## 🧪 Примеры тестирования

### Тест 1: Проверка авторизации

1. **Get Current User** с ADMIN токеном
   - Ожидается: 200 OK, данные пользователя
   
2. **Get Current User** без токена
   - Ожидается: 401 Unauthorized

3. **Admin Only Endpoint** с USER токеном
   - Ожидается: 403 Forbidden

### Тест 2: CRUD операции с продуктами

```
1. List Products (GET)
2. Create Product (POST) - получить ID
3. Get Product Version (GET) с ID из шага 2
4. Update Product Version (PUT)
5. Create New Version (POST)
6. Delete Product Version (DELETE)
7. Delete Product (DELETE)
```

### Тест 3: Работа с файлами

```
1. Create File Metadata (POST) - получить file_id
2. Upload File (POST) с multipart/form-data
3. Download File (GET)
4. Process File (POST) с переменными
5. Delete File (DELETE)
```

### Тест 4: Калькулятор и коэффициенты

```
1. Create Calculator (POST)
2. Get Calculator (GET)
3. Create Coefficients (POST)
4. Get Coefficients (GET)
5. Replace Coefficients (PUT)
6. Sync Variables (POST)
```

## 📝 Переменные окружения

Вы можете изменять следующие переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `base_url` | URL сервера | `http://localhost:8080` |
| `jwt_token` | Активный JWT токен | `eyJhbGc...` |
| `admin_token` | Токен ADMIN | `eyJhbGc...` |
| `user_token` | Токен USER | `eyJhbGc...` |
| `product_code` | Код продукта | `TEST_PRODUCT` |
| `product_id` | ID продукта | `1` |
| `version_no` | Номер версии | `1` |
| `package_no` | Номер пакета | `1` |
| `calculator_id` | ID калькулятора | `1` |
| `file_id` | ID файла | `1` |
| `lob_code` | Код LOB | `AUTO` |
| `policy_number` | Номер политики | `POL-12345` |

## 🐛 Troubleshooting

### 401 Unauthorized

**Причина:** Токен отсутствует, невалиден или истёк

**Решение:**
1. Проверьте, что токен установлен в Environment
2. Убедитесь, что токен не истёк (проверьте `exp` claim на jwt.io)
3. Убедитесь, что пользователь существует в БД

### 403 Forbidden

**Причина:** Недостаточно прав для выполнения операции

**Решение:**
1. Для `/admin/**` endpoints нужна роль ADMIN
2. Проверьте роль в токене (claim `role`)
3. Используйте `admin_token` вместо `user_token`

### 404 Not Found

**Причина:** Ресурс не найден или ID неверный

**Решение:**
1. Проверьте ID в URL
2. Убедитесь, что ресурс существует (сначала создайте его)
3. Проверьте правильность пути

### Cannot resolve host

**Причина:** Сервер не запущен или неверный URL

**Решение:**
1. Убедитесь, что приложение запущено: `./gradlew bootRun`
2. Проверьте, что `base_url` указывает на правильный адрес
3. Проверьте порт (по умолчанию 8080)

## 🚀 Быстрый старт

1. **Импортируйте коллекцию и environment**
2. **Создайте JWT токен на jwt.io**:
   ```json
   {
     "sub": "admin",
     "exp": 1999999999
   }
   ```
3. **Обновите переменную `jwt_token`** в Environment
4. **Запустите приложение**: `./gradlew bootRun`
5. **Протестируйте Health Check** (без авторизации)
6. **Протестируйте Get Current User** (с авторизацией)
7. **Начинайте работу с API!**

## 📚 Дополнительные ресурсы

- **JWT авторизация:** `pt-auth/JWT_README.md`
- **Проверка прав в контроллерах:** `pt-launcher/SECURITY_CONTROLLERS.md`
- **Система авторизации:** `pt-auth/SECURITY_README.md`

## ✅ Checklist перед использованием

- [ ] Postman установлен
- [ ] Коллекция импортирована
- [ ] Environment импортирован и активирован
- [ ] JWT токен сгенерирован и установлен
- [ ] Пользователь создан в БД (логин из токена)
- [ ] Приложение запущено на localhost:8080
- [ ] Health Check возвращает 200 OK
- [ ] Get Current User возвращает данные пользователя

---

**Дата создания:** 18 ноября 2025  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию

