name: Деплой на сервер через SSH

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🔄 Получение обновлений из репозитория
        id: pull-code
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd PtDocker
            echo "📥 Получаем последние изменения из репозитория..."
            git pull
            echo "✅ Код успешно обновлен"
            echo "Текущий статус репозитория:"
            git status

      - name: 🏗️ Сборка Docker контейнеров
        id: build-containers
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd PtDocker
            echo "🔨 Собираем Docker контейнеры..."
            docker compose build --no-cache --pull
            echo "✅ Сборка Docker завершена успешно"
            echo "Доступные образы:"
            docker images | head -10

      - name: 🚀 Запуск Docker контейнеров
        id: start-containers
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd PtDocker
            echo "🚀 Запускаем Docker контейнеры..."
            # Останавливаем старые контейнеры перед запуском новых
            docker compose down
            # Запускаем новые контейнеры
            docker compose up -d
            echo "✅ Docker контейнеры успешно запущены"
            echo "Запущенные контейнеры:"
            docker ps

      - name: 🔍 Проверка деплоя
        id: verify-deployment
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            echo "🔍 Проверяем состояние деплоя..."
            echo "Статус Docker контейнеров:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
            
            echo -e "\nЛоги контейнеров (последние 5 строк):"
            for container in $(docker ps -q); do
              container_name=$(docker inspect --format='{{.Name}}' $container | sed 's/\///')
              echo "=== $container_name ==="
              docker logs --tail=5 $container 2>/dev/null || echo "Логи недоступны"
            done
            
            echo -e "\n✅ Проверка деплоя завершена успешно"